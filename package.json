{
  "name": "local-outreach-dashboard-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-driven factory for locally hosted role and creator outreach dashboards.",
  "scripts": {
    "test": "node --test",
    "create-instance": "node src/create-instance.js"
  }
}
