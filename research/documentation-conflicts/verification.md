# Verification — 2026-09-18

Pricing and v4 policy values checked directly; conflicting values preserved. v2 reference explicitly excluded as current quota authority. Web-only evidence identified separately.

All relative Markdown links resolve. Branch starting HEAD verified equal to d757e94.
No executable application changes; application tests are not relevant to these reports.
Coverage audit script is a read-only research utility. No paid/account endpoint tested.

Website snapshots that were retrievable are retained in the local /tmp research cache;
the repository stores retrieval metadata, hashes and selected observations. A hash
alone is not a full historical snapshot. Web-tool-only findings have URLs, sections
and short excerpts but no byte digest. StatsBomb repository URLs are commit-pinned.

The repository file inventory and SHA-256 values are in artifact-manifest.json.
